# Android project files

Generate the Android host project with `flutter create .` from this directory. Keep only the minimum permissions required for QR scanning, secure storage, and reading the connected Wi-Fi BSSID.
